{
  "name": "ImFx",
  "version": "4.0.0",
  "description": "A series of client-side image processing experiments",
  "scripts": {
    "cleanup": "rm -rf .cache dist",
    "dev": "npm run cleanup && parcel src/index.html",
    "build": "npm run cleanup && parcel build src/index.html --public-url ./ --no-source-maps"
  },
  "keywords": [
    "image",
    "processing",
    "kernel"
  ],
  "author": "Neil Syiemlieh",
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.17.11"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "parcel-bundler": "^1.12.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mebble/imfx.git"
  }
}
