{
  "name": "ImFx",
  "version": "1.0.0",
  "description": "Apply a filter to an image through kernel convolution",
  "scripts": {
    "cleanup": "rm -rf .cache dist",
    "dev": "npm run cleanup && parcel src/index.html",
    "build": "npm run cleanup && parcel build src/index.html && rm dist/*.map && mv dist build"
  },
  "keywords": [
    "image",
    "processing",
    "kernel"
  ],
  "author": "Neil Syiemlieh",
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.17.11",
    "p5": "^0.7.3"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "parcel-bundler": "^1.12.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mebble/imfx.git"
  }
}
