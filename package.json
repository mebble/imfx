{
  "name": "ImFx",
  "version": "1.0.0",
  "description": "Apply a filter to an image through kernel convolution",
  "scripts": {
    "rm-dist": "rm -rf dist",
    "build-assets": "npm run rm-dist && mkdir dist && cp -r assets/* dist",
    "dev": "npm run build-assets && parcel src/index.html",
    "build": "npm run build-assets && parcel build src/index.html && rm dist/*.map && mv dist build"
  },
  "keywords": [
    "image",
    "processing",
    "kernel"
  ],
  "author": "Neil Syiemlieh",
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.17.11",
    "p5": "^0.7.3"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "parcel-bundler": "^1.12.2"
  }
}
